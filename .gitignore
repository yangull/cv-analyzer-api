# Virtual environment — never commit this
venv/

# Python cache files — auto-generated by Python, not needed in git
__pycache__/
*.pyc

# Test PDFs — don't accidentally commit personal CV files
*.pdf

# Environment variables file — if you ever create one
.env